# ai-infra-labs — layered requirements (match ai-infra-docs L0.6 baseline)
# Install: pip install -r requirements.txt
# GPU path (CUDA 11.7+): pip install torch --index-url https://download.pytorch.org/whl/cu118

# --- L0/L1 baseline ---
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
pybind11>=2.11
pandas>=2.0

# --- L2 ---
triton>=2.0; platform_system=="Linux"

# --- L3 (optional multi-GPU) ---
deepspeed>=0.14

# --- L4/L5 ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
mlflow>=2.10
prometheus-client>=0.19
sentence-transformers>=2.5
qdrant-client>=1.9

# --- L4 heavy (install separately if needed; large downloads) ---
# transformers>=4.40
# autoawq>=0.2
# auto-gptq>=0.7
# vllm>=0.6
# openai>=1.0
