# ai-infra-labs — layered requirements (match ai-infra-docs L0.6 baseline)
# Install PyTorch first (pick ONE wheel matching your driver):
#   pip install torch --index-url https://download.pytorch.org/whl/cu126   # CUDA 12.6 (production stable)
#   pip install torch --index-url https://download.pytorch.org/whl/cu130   # CUDA 13.0 (newer GPUs / verified labs)
# Then: pip install -r requirements.txt

# --- L0/L1 baseline ---
numpy>=1.26
scikit-learn>=1.4
matplotlib>=3.8
pybind11>=2.12
pandas>=2.1

# --- L2 ---
triton>=3.0; platform_system=="Linux"
onnx>=1.16
onnxscript>=0.1.0
onnxruntime>=1.19

# --- L3 (optional multi-GPU) ---
deepspeed>=0.16

# --- L4/L5 ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
mlflow>=2.18
prometheus-client>=0.21
sentence-transformers>=3.0
qdrant-client>=1.12
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
ray[default]>=2.37

# --- L4 heavy (install in .venv-sys with Python 3.12) ---
# transformers>=4.46
# autoawq>=0.2
# gptqmodel>=2.0
# optimum>=1.23
# torchvision
# vllm>=0.8
# bitsandbytes>=0.45
